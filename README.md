# zaikolot

Zaiko 抽选页面辅助扩展。

## 当前版本

`0.1.2`

这是一个仍在验证阶段的版本。扩展只在开启开关且匹配到 Zaiko 页面时运行，核心流程为勾选页面 checkbox、点击申请、确认弹窗并进入抽选状态页面。

## 版本规则

- `0.x.y`：功能仍在验证阶段
- `x.y.0`：增加功能或出现不兼容的行为变化
- `x.y.z`：修复问题，不改变核心使用方式

Git 标签与 `manifest.json` 中的扩展版本保持一致，例如 `v0.1.1`。
